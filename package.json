{
  "name": "flash-ticket-engine",
  "version": "1.0.0",
  "private": true,
  "description": "High-Concurrency Flash-Sale & Seat Reservation Engine",
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "node run-dev.js",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "stress-test": "npm --prefix server run stress-test"
  }
}
